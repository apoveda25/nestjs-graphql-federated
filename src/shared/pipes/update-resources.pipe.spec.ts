import { UpdateResourcesPipe } from './update-resources.pipe';

describe('UpdateResourcesPipe', () => {
  it('should be defined', () => {
    expect(new UpdateResourcesPipe()).toBeDefined();
  });
});
