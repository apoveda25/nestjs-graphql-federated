import { CreateResourcesPipe } from './create-resources.pipe';

describe('CreateResourcesPipe', () => {
  it('should be defined', () => {
    expect(new CreateResourcesPipe()).toBeDefined();
  });
});
