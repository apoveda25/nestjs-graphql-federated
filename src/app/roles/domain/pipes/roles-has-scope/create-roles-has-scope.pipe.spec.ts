import { CreateRolesHasScopePipe } from './create-roles-has-scope.pipe';

describe('CreateRolesHasScopePipe', () => {
  it('should be defined', () => {
    expect(new CreateRolesHasScopePipe()).toBeDefined();
  });
});
