import { RolesUpdatedEventHandlers } from './roles-updated.handlers';

describe('RolesUpdatedEventHandlers', () => {
  it('should be defined', () => {
    expect(new RolesUpdatedEventHandlers()).toBeDefined();
  });
});
